'use strict';

/**
 * Example:
 * 
 * module.exports = [
 *  SOME_SHOPIFY_PRODUCT_ID,
 *  ANOTHER_SHOPIFY_PRODUCT_ID
 * ]
 * 
 * Usage: Clear the array after script runs successfully
 *  so that the array is empty
 *  make sure numbers are surrounded by quotations: e.g. '123'
 */

module.exports = [

];